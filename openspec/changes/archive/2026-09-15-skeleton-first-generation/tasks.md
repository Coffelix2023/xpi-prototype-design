## 1. 更新 SKILL.md §7 产出章节

- [x] 1.1 在 §7 产出章节下新增 "### 生成策略" 小节，插入在 "### wireframe" 之前，明确骨架优先原则、复杂度红线（单次输出 ≤ 200 行）、工具选择规则（`write` vs `replace` vs `insert`），验证方法：读取修改后的 SKILL.md §7，确认新小节存在且位置正确
  验证: 新小节插入在 §7 第 245 行，位于 "### wireframe" 之前，包含骨架优先原则和工具选择规则 · 2025-01-09
- [x] 1.2 在 "生成策略" 小节中添加骨架完整性标准：合法 `<!DOCTYPE>`、完整 `<head>`、闭合 `<body>`、空 `<style>`/`<script>` 标签、所有语义区块占位符带 `data-block` 属性，验证方法：小节内容包含所有 5 个标准点
  验证: 骨架阶段第 1 条包含 `<!DOCTYPE>` + `<head>` + `<body>`，第 2 条包含 `<style>` / `<script>`，第 3 条包含 `data-block` 属性 · 2025-01-09
- [x] 1.3 在 "生成策略" 小节中添加分块填充规则：CSS tokens → 语义区块 → 交互脚本的阶段顺序，单次 `replace` 输出 ≤ 200 行，若超过则拆分，验证方法：小节明确列出三个阶段及 200 行红线
  验证: 填充阶段列出 CSS tokens / 区块内容 / 交互脚本三个阶段，复杂度红线章节明确指出 200 行限制 · 2025-01-09
- [x] 1.4 在 "生成策略" 小节中添加工具选择约束：骨架用 `write`，填充用 `replace`/`insert`，禁止为小改动而 `read` + `write` 整文件，验证方法：小节包含三条工具选择规则
  验证: 工具选择章节包含骨架用 `write` / 填充用 `replace`/`insert` / 禁止为小改动 `read` + `write` 三条规则 · 2025-01-09
- [x] 1.5 在 "生成策略" 小节中添加复杂度豁免规则：纯声明式 HTML（表单、表格）可豁免，CSS 嵌套 > 3 层或 JS 函数 > 50 行必须拆分，SVG 图标定义不计入红线但需独立 `<defs>` 区块，验证方法：小节列出三类豁免/强制拆分场景
  验证: 复杂度豁免章节包含 HTML 模板豁免 / CSS 嵌套与 JS 函数强制拆分 / SVG 图标定义豁免三类场景 · 2025-01-09

## 2. 更新 SKILL.md §5.2 执行腿章节

- [x] 2.1 在 SKILL.md §5.2 执行腿的动作清单中，在原来的第 4 步之前插入骨架优先策略：若任务需创建新 HTML 且预估超 200 行，必须先 `write` 骨架，再 `replace`/`insert` 填充，验证方法：新的第 4 步包含两小步拆分逻辑
  验证: 执行腿第 4 步已更新为骨架优先策略，包含骨架生成、逐块填充两小步，原第 4-6 步顺延为第 5-7 步 · 2025-01-09
- [x] 2.2 在 §5.2 执行腿章节补充一个完整的"按构建阶段拆分"任务序列示例，包含：生成骨架 → 填充 CSS tokens → 实现各语义区块 → 实现交互脚本，每个任务带验收标准与产出路径，验证方法：示例包含至少 4 个任务，每个任务有 `(验收:...;产出:...)` 标注
  验证: §5.2 第 7 步后新增示例，包含 5 个任务（1.1 生成骨架 → 1.2 填充 CSS tokens → 1.3 实现 header → 1.4 实现 main → 1.5 实现交互脚本），每个任务都有验收标准和产出路径 · 2025-01-09
- [x] 2.3 在示例任务中明确体现骨架优先原则：第一个任务产出骨架（150 行左右），后续任务逐块填充（每次 ≤ 200 行），验证方法：示例任务序列的第一个任务标题包含"骨架"字样
  验证: 示例第一个任务 1.1 标题为"生成首页骨架"，后续任务 1.2-1.5 为逐块填充（CSS tokens、header、main、交互脚本），符合骨架优先原则 · 2025-01-09

## 3. 验证新策略与现有约束的一致性

- [x] 3.1 检查修改后的 §7 生成策略与 §2.7 无构建规则的一致性，确认仍为单文件自包含 HTML，验证方法：grep SKILL.md 确认 §7 未引入构建步骤或外部依赖
  验证: grep 搜索 §7 未出现 webpack/vite/rollup/bundler/compile/transpile 等构建工具关键词，§2.7 第 63 行明确规定"无构建、单文件自包含 HTML"，§7 新增的"增量构建"指的是 Agent 的生成步骤而非工具链构建，一致 · 2025-01-09
- [x] 3.2 检查修改后的 §5.2 示例与 §5.1 闸门机制的兼容性，确认任务拆分不绕过 `prototype_gate` 授权，验证方法：§5.2 示例中未出现绕过闸门的描述
  验证: §5.2 示例任务序列（1.1-1.5）都在闸门放行后执行，示例第 191 行说明"每个任务用 replace 或 insert"，未出现绕过闸门或用 bash 写文件的描述，§5.1 第 55/131 行的闸门机制未被架空，兼容 · 2025-01-09
- [x] 3.3 检查修改后的内容与 §8 快照机制的一致性，确认分阶段任务仍在统一快照下，验证方法：grep SKILL.md 确认 §7/§5.2 未改变快照时机（仍为"全部任务完成后"）
  验证: §5.2 第 175 行"直到全部完成，然后按 §8 快照 + 预览"，§5.1 第 128 行"收尾照 §8 快照 + 预览"，§8 第 322 行定义快照在产出后调用，§7 新增的分阶段任务策略未改变这个时机（每轮仍是全部任务完成后一次性快照），一致 · 2025-01-09

## 4. 文档整体一致性检查

- [x] 4.1 运行 `pnpm typecheck` 确认 TypeScript 类型检查通过，验证方法：命令输出无错误
  验证: `tsc --noEmit` 输出为空、退出码 0，无类型错误 · 2025-01-09
- [x] 4.2 运行 `pnpm -w run lint` 确认 Biome 检查通过，验证方法：命令输出无错误
  验证: `biome check .` 退出码 0；`Found 13 warnings / 88 infos`，全部落在 `src/*.ts`（tools.ts 34、gate.ts 14、migration.ts 12、index.ts 10 等），含 `no-secrets` 对中文卡面文案的误报；本轮改动只触及 SKILL.md 与 openspec 目录，非新增问题 · 2025-01-09
- [x] 4.3 运行 `pnpm test` 确认现有单元测试通过（若有），验证方法：命令输出所有测试通过或提示无测试文件
  验证: `vitest run --passWithNoTests` → 9 个测试文件、139 个测试全部通过 · 2025-01-09
- [x] 4.4 阅读修改后的 SKILL.md 全文，确认新增内容与其他章节术语一致（如 `current/`、`vN/`、`plan.md`、`tasks.md`），验证方法：新增章节使用的路径和术语与 §2/§4/§8 一致
  验证: 通读 381 行，发现并修掉 2 处新增内容引入的术语冲突：(1) §7 生成策略与 §5.2 第 4 步自造 `data-block` 属性，而 §7 `### wireframe` 与 `src/templates.ts` 的既有契约是 `data-wireframe-block`，两处已改为 `data-wireframe-block`（第 171、267 行）；(2) §5.2 示例末尾「每个任务用 `replace` 或 `insert`」与同段第 4 步「骨架用 `write`」矛盾，已补「除 1.1（骨架用 `write`）外」的例外（第 188 行）。其余术语核对一致：`current/`（26 处）、`plan.md`（14）、`tasks.md`（13）、`gate.json`（4）、`THEMES.md`（6）、`DELTA.md`（3）、`CHANGELOG.md`（4）；文档版本目录统一写作 `vN`（无斜杠，第 154/155/331/372 行），本轮新增内容未引入版本路径，故无需改写。`vN/` 在 SKILL.md 本就不存在，非本轮回归 · 2025-01-09
